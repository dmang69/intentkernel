# intentkernel
IntentKernel is a universal computing security architecture that replaces the 55-year-old permission-based security model with event-scoped capability tokens derived from verified user intent. No process has any default authority. All capabilities are granted for exactly one action, at the exact moment of user intent, and expire automatically.
